FAIRY STAR MISSIONS — IPAD LAUNCH PACKAGE (1.7.1)

Open START HERE.html for the guide.

website/ — upload ONLY this folder to your private HTTPS host.
Mac - clear test progress.html — optional local practice/reset helper. Do not upload it.

A new iPad installation starts with zero stars. Existing browser records are not inside the app files.
To keep customizations, export a backup from the original preview, restore it in the installed app,
then use room name > Bedtime tidy-up > Backups > Clear test progress (type START).
Reset keeps mission/reward definitions, settings, pins and photo album photos.
No automatic reset occurs on launch, update, or midnight.

This package has not been uploaded to a host or installed on your iPad.
Keep the live URL private, including production deployments, and do not upload backup JSON files.
Use the installed icon, wait for Offline ready in Backups, and save weekly backups outside the app.

SOURCE AND CHECKS
All JavaScript/CSS source is in website/; there is no build step needed to host it.
The deployment includes the original Round 7 art and all original verified animation frames.
130 code tests and 15 targeted browser checks passed for this launch build.
Browser checks used a test storage adapter, not real iPad persistence.
